using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using CardGame3Lib;
using System.Collections.Generic;

namespace UnitTestCardGame3Lib
{
    [TestClass]
    public class UnitTestCardPackageProvider
    {
        //[TestMethod]
        //public void TestGetCardPackage()
        //{
        //    CardPackageProvider Cpp = new CardPackageProvider();
        //    List<Card> listOfCards = new List<Card>();
        //    for (int i = 1; i <= 32; i++)
        //    {
        //        listOfCards.Add(new Card(i));
        //    }
        //    CardPackage expectedCardPackage = new CardPackage(listOfCards);

        //    Assert.AreEqual(expectedCardPackage, Cpp.GetCardPackage());
        //}
    }
}
